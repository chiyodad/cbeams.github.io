<feed xmlns="http://www.w3.org/2005/Atom">
 <title>cbeams/{{ collection_name }}</title>
 <link href="{{ site.feedurl }}/{{ collection_path }}/atom.xml" rel="self"/>
 <link href="{{ site.url }}/{{ collection_path }}"/>
 <id>{{ site.url }}</id>
 <updated>{{ site.time | date_to_xmlschema }}</updated>
 <author>
   <name>{{ site.author.name }}</name>
   <email>{{ site.author.email }}</email>
 </author>
 {% for post in collection_posts %}
 <entry>
   <title>{{ post.title }}</title>
   <link href="{{ site.url }}{{ post.url }}"/>
   <id>{{ site.url }}{{ post.id }}</id>
   {% for collection in post.categories %}
   <category term="{{ collection_name }}"/>
   {% endfor %}
   <published>{{ post.date | date_to_xmlschema }}</published>
   {% if post.updated %}
   <updated>{{ post.updated | date_to_xmlschema }}</updated>
   {% else %}
   <updated>{{ post.date | date_to_xmlschema }}</updated>
   {% endif %}
   <content type="html">{{ post.content | xml_escape }}</content>
 </entry>
 {% endfor %}
</feed>
